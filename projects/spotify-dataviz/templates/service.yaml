apiVersion: v1
kind: Service
metadata:
  name: {{ include "spotify-dataviz.fullname" . }}
  labels:
    {{- include "spotify-dataviz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spotify-dataviz.selectorLabels" . | nindent 4 }}
